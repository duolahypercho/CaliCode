# CaliCode soak bug ledger

This ledger records failures reproduced while building `Neon Coin Dash QA`
through the native app and a second browser frontend. It is intentionally
evidence-first: items that were observed only once remain marked for soak
reproduction rather than being promoted to confirmed root causes.

| ID | Severity | Area | Reproduction / evidence | Status |
| --- | --- | --- | --- | --- |
| CALI-SOAK-001 | P0 | Editor bridge | With Chrome and the native app connected to the same core, a fresh Neon chat repeatedly received `editor is attached to another session/worktree`; PIE and test tools were rejected until the duplicate frontend closed. | Fixed with per-session client ownership; unit/RPC/E2E coverage green, final native two-window check pending unlock |
| CALI-SOAK-002 | P0 | Agent lifecycle | Both `deepseek-v4-flash` and `gpt-5.6-luna` spent most of a 12-turn budget on discovery/retries, then terminated with either `Turn limit reached before the agent finished.` or `I could not produce a response.` without a useful recovery action. | Fixed with empty-response retry, explicit terminal metadata, resumable max-turn result, and 20-turn client budget; core tests green |
| CALI-SOAK-003 | P1 | Entity inspector | Controlled numeric fields collapse intermediate input. Typing a leading minus produced positive coordinates, and one decimal edit produced `35` instead of `0.35`, creating a wall-sized entity. | Fixed; focused component tests and repeated Playwright signed/decimal persistence workflows green |
| CALI-SOAK-004 | P1 | Responsive editor | Entity properties are hidden at narrower effective workspace widths, leaving selection without an editable inspector while the agent panel is open. | Fixed; responsive component and full visual/browser suites green |
| CALI-SOAK-005 | P1 | Dev server | Vite emitted reloads for `src-tauri/target/**`, bundled app resources, and copied `dist/index.html` files during desktop builds, interrupting the browser session and flooding terminal output. | Fixed with generated-path watch exclusions and managed dev-process lifecycle; custom-port start/stop verified |
| CALI-SOAK-006 | P1 | Core recovery | When the pre-existing core disappeared, the browser silently fell back to Starter and hid the saved Neon project until a controlled core restart and reload. The project remained safe on disk. | Fixed with explicit offline/retry state, active-project preservation, reconnect probing, and dirty autosave retry; transport tests green, final native restart check pending unlock |
| CALI-SOAK-007 | P1 | Cross-window state | During the attachment conflict, the native app showed a Starter header with the Neon session's tool transcript, while Chrome showed Neon. Project/session/editor ownership was not legible to the user. | Fixed in routing/state contracts; multi-session ownership tests green, final native two-window check pending unlock |
| CALI-SOAK-008 | P2 | Playtest state | A fresh native session displayed `2/2 passing · 0 frames captured` before an explicit rerun; the rerun correctly produced 10 frames. | Covered by repeated play/reset/playtest browser workflows; no stale-pass reproduction after fix wave |
| CALI-SOAK-009 | P2 | Runtime status | A background Chrome frontend could remain `SIG RUNNING` with `FPS 0`; Reset/Play recovered state, while the foreground native app held 59–60 FPS. | Fixed: hidden editors report `SIG BACKGROUND` instead of implying a runtime stall; visibility regression test green |
| CALI-SOAK-010 | Watch | Core process | The initially discovered core process exited during the first runtime attempt. A controlled `cargo run` core remained stable through later runs, so this is not yet attributable to a product crash. | Not reproduced across full and repeated isolated suites; retain as watch item for longer native soak |
| CALI-SOAK-011 | P1 | Generated workspaces | `client/worktrees/` held 14 generated worktrees (33 MB) plus `client/sessions/` inside Vite's project root. Without explicit watch exclusions, session churn can multiply filesystem events and reload pressure. | Fixed for new runs: generated paths are ignored and Playwright state is isolated under disposable `.e2e-state`; historical user artifacts preserved |
| CALI-SOAK-012 | P1 | Model controls | The UI displays and persists reasoning effort such as `gpt-5.6-luna · max`, but `AgentPanel` documents that core currently ignores the `effort` parameter. The control therefore overstates the request actually sent. | Fixed; effort reaches request-scoped provider payload and regression tests pass; live provider confirmation needs a configured key |
| CALI-SOAK-013 | P0 | Editor tool surface | `editor_scene_inspect` claims to inspect scripts and tests but returns only their ids/names, omits entity `scriptIds` and materials, and `editor_test_add` cannot update an existing test. Agents consequently burn turns searching worktree files for live project code and cannot safely replace tests. This directly preceded both terminal agent failures. | Fixed with complete snapshots, validated object updates, idempotent script/test upserts, and focused tests |
| CALI-SOAK-014 | P0 | Session resources | Durable session create/fork allocates generated git worktrees, but session/project deletion and project-session archive do not remove them; failed allocation can also leave empty session records. Fourteen orphan worktrees were present after prior runs. | Fixed with clean generated-worktree cleanup, dirty-worktree preservation, rollback, editor/agent cancellation, and RPC tests |
| CALI-SOAK-015 | P0 | Session persistence | Session save/fork/compaction/delete can run concurrently, but the original read-modify-write path was not serialized and reused one temporary filename. A late writer could replace a newer transcript or collide with another save during a long agent run. | Fixed with serialized session I/O and unique atomic temporary files; concurrency tests green |
| CALI-SOAK-016 | P1 | E2E isolation | Parallel repeated workflow workers generated identical `Date.now()` names, and core-derived E2E sessions/worktrees lived beside `client/`, so an interrupted live test leaked three durable records and worktrees. | Fixed with worker/repeat/random labels plus one disposable `.e2e-state` root; 5x parallel workflow repeat and full 58-test suite green |
| CALI-SOAK-017 | P1 | Desktop packaging | `pnpm desktop:build` produced the app but failed the DMG when macOS denied create-dmg permission to send cosmetic AppleEvents to Finder. | Fixed with a narrow CI-safe DMG fallback after the styled packaging attempt; the exact desktop build command now succeeds and emits both bundles |
| CALI-SOAK-018 | P0 | Offline recovery | After an autosave failed offline, clicking `Retry` reran startup hydration and replaced the newer dirty in-memory project with core's older disk copy before autosave could retry. | Fixed so hydrated games probe transport without reopening; a real routed-RPC outage/reconnect/persistence test and 3x repeated workflow suite are green |
| CALI-SOAK-019 | P0 | Cross-game session state | Creating `Native Soak 0811` while a Neon task was selected carried that session id into the new game, then rendered `Resume failed: session belongs to project Some("neon-coin-dash-qa"), not native-soak-0811`. | Fixed by clearing/remounting task state on every game-creation/open path; regression E2E added, full isolated rerun pending native-port release |
| CALI-SOAK-020 | P0 | Compaction persistence | `session_save` rebuilt the durable record without its `archived` field, so the first ordinary transcript autosave after compaction could erase the soft-archived turns that compaction promised to preserve. | Fixed by preserving archived turns across saves with a direct save/archive/save regression test |
| CALI-SOAK-021 | P1 | Agent activity UX | Every tool call rendered as a separate low-context row, work duration was lost on completion/resume, concurrent same-name calls paired by name, and file edits could not open the real file or a trustworthy diff in the editor. | Fixed: one collapsed activity per Enter, call-id pairing, atomic bounded edit metadata, worked-time persistence, aggregate file stats, expanded action details, and safe click-through to the real file/diff; 307 unit and 62 isolated E2E tests green |
| CALI-SOAK-022 | P0 | Native workspace access | The packaged sidecar could serve the app but `workspace_open` against a repo under macOS Desktop consistently timed out while the same path opened in ~2ms from the user's shell. The custom browser-style picker never produced an NSOpenPanel grant, so protected folders could not back the real editor. | Native picker and explicit access-recovery flow implemented with scoped directory selection; Tauri fmt/clippy/tests and browser fallback tests green. Packaged parent-to-sidecar grant behavior still needs the final Computer Use pass after the Mac is unlocked; broker fallback remains the next fix if the OS does not extend the grant to the sidecar. |
| CALI-SOAK-023 | P1 | Loop report evidence | `report.html`/`report.md` are written to `<project>/reports/loops/<loop-id>/`, but evidence paths are recorded project-relative, so every capture link resolved inside the loop directory and 404'd. Screenshots and contact sheets were also emitted as bare `<a href>` links, so a progress report never actually showed the frames it was reporting on. | Fixed: evidence URLs hop up to the project root, and image/video evidence embeds inline (`<img>`/`<video>` in HTML, `![]()` in Markdown). Verified end to end by driving an isolated core over RPC and screenshotting the rendered report — `naturalWidth` non-zero, zero failed requests. 545 Rust tests green. |
| CALI-SOAK-024 | Blocked | Live model access | The whole live acceptance path is stalled upstream of CaliCode: `codex-router` on `:4100` answers `/health/liveliness` but returns `{"error":"No connected db."}` for every `/v1` call, and the last real loop run (`luna-native-soak-0812/loop-mspz897a`) died at iteration 1 on a provider `429 GoUsageLimitError — weekly usage limit reached` for `gpt-5.6-luna`. Ollama is up but has zero models pulled, so there is no local fallback. | Not a product defect — no CaliCode code path is implicated. Blocks one-prompt game acceptance, 3D-asset agent testing, and any judge/critic scoring. A monitor is armed on `:4100` to resume automatically once it serves. |
| CALI-SOAK-025 | P1 | Blender asset path | `blender.rs` had no entry in `tools.rs`, so the agent could not reach Blender at all — the "spawn an agent to build assets in Blender" path did not exist. `open()` is also fire-and-forget: it launches a windowed Blender and returns before any GLB is written, so an agent calling it would report an asset that does not exist. | Fixed: added `blender::export`, a headless `--background` run that awaits the bridge's export, treats "exited 0 but wrote no GLB" as a failure, bounds stderr for model context, and kills the child on timeout. Exposed as RPC `blender_asset_export` and an agent tool. Verified end to end against real Blender 5.1.2: `ready:false/0 bytes` → 69,684-byte GLB with `glTF` magic, parsed to confirm mesh `Suzanne` at 1,966 vertices. |
| CALI-SOAK-026 | P1 | Prompt caching | Claude routed through OpenRouter or codex-router receives **no caching signal at all**. `sends_cache_key` (`model.rs:418`) requires `supports_prompt_cache_key`, which `provider_capabilities` grants only to direct OpenAI or codex-router *Luna*; the long-retention escape needs `CacheRetention::Long`, but the default is `Short` (`model.rs:1262`). There are also zero `cache_control` breakpoints in the tree. OpenRouter does not cache Anthropic models automatically — the caller must supply `cache_control` — and LiteLLM's `enable_anthropic_prompt_caching` applies only to `anthropic/`/`bedrock/` prefixes, not generic OpenAI-compatible endpoints. | Fixed for OpenRouter: `marks_anthropic_cache_control` + `apply_anthropic_cache_control` place two `cache_control` breakpoints — closing the system prompt and rolling on the newest user turn — mirroring Pi's layout and staying under Anthropic's four-breakpoint ceiling. `role: "tool"` is never marked (a marker there hangs OpenRouter per Hermes #57845) and empty content is skipped (Anthropic rejects empty text blocks). Deliberately **not** applied to codex-router: if its LiteLLM config already injects markers upstream, adding ours would exceed the ceiling and fail the call. Three tests, including one asserting the captured wire body carries markers for `anthropic/claude-sonnet-4-5` and does **not** for `openai/gpt-4o`. Live provider confirmation still pending — no key and `:4100` is down, so this is verified at the wire-format layer only. |

| CALI-SOAK-027 | P0 | Agent-run visual tests | `editor_run_tests` called `runTests` without a `baselineCompare`, so `baseline()` in an agent-authored test resolved `{pass: true, distance: 0}` without comparing anything. An agent could write a visual regression test, run it, get green, and report the scene verified — a fabricated pass that reads as evidence. | Fixed: the agent's runner now passes the same `test_baseline_compare` RPC the TESTS panel uses, fail-closed to `{pass: false, distance: 64}` when unreachable. The missing-comparator fallback now throws instead of synthesising a pass. Regression test asserts a comparator-less run fails. `docs/game-harness.md` corrected — it had documented the no-op as a standing gap. |
| CALI-SOAK-028 | P1 | Test sandbox diagnostics | All three sandboxes (in-process, worker, CSP frame) recorded a failed capability call into `capabilityError` but resolved `undefined` into the script, so the script's downstream `TypeError` threw first and `drainCalls()` — the only code that surfaces `capabilityError` — never ran. The reported failure was `Cannot read properties of undefined (reading 'pass')` instead of the real cause. | Fixed in all three: a script rejection now reports `capabilityError` when one was recorded. Found while fixing CALI-SOAK-027 — the masking is what made the fabricated baseline pass hard to see. |

| CALI-SOAK-029 | Resolved | Live-path testability | Five iterations of loop work could not exercise the agent loop at all: `codex-router` on `:4100` has no DB and `gpt-5.6-luna` is weekly-capped, so every live acceptance claim was blocked on someone else's infrastructure. | Resolved as a *testability* problem rather than waiting: `scripts/mock-model.py` is a scripted OpenAI-compatible provider (core skips the key check for `127.0.0.1`), which exercises the real `agent_chat` path end to end. First run proved parallel subagent fan-out with a 2 ms start skew and a 1.513 s wall span against a 4.5 s sequential floor. Live *model quality* still needs a real provider; live *control flow* no longer does. |
| CALI-SOAK-030 | Verified | Full loop architecture | The central claim — one prompt fans out to parallel specialists, each is monitored, a blind judge scores against a reference, and failures re-queue builders with a punch list — had never been observed end to end; only its parts were unit-tested. | Verified in one `agent_chat` via `scripts/mock-model.py graph`, whose judge scores 70 then 95 so the run can only complete if rejection re-queues. Result: 15 model calls (3 parent, 5 build-node, 5 monitor, 2 judge), three roots concurrent at 5 ms skew, integration correctly waiting on all three deps, judge #1 rejecting at 70, a builder re-running with the punch list, judge #2 passing at 95, `graph_status` 5/5 passed. Monitors receive 0 tools; the judge receives the full surface. |
| CALI-SOAK-031 | P1 | Fan-out model routing | Every agent in a fan-out ran the one model in `model.default` — `AgentOptions` carried no model at all, so a build subagent, the per-node monitor, and the blind judge grading them were always the same model. A judge that shares its builders' weights also shares their blind spots, and there was no way to put cheap builders on one model and a strong critic on another. | Fixed: `model.roles` maps a role to `model` or `provider/model`, resolved by `config::apply_role_model` at the three places core clones config for a call (agent turn, finalization drain, node monitor). The role comes from the spawn (`subagent_spawn`/graph node role, plus reserved `judge`/`monitor`), never from tool arguments — a slash prefix counts as a provider only when it names a configured preset, so `anthropic/claude-sonnet-4-5` survives intact. Fixed: `model.roles` maps a role to `model` or `provider/model`, resolved by `config::apply_role_model` at the four places core clones config for a call (agent turn, finalization drain, node monitor, goal verifier). Role keys are ordered most-specific-first, because a judge node is `critic` by plan role and `judge` by engine kind; the engine offers the extra key through `_modelRoleHint`, honoured only on spawns carrying its private graph binding, so the same key in model-authored arguments is ignored. **The live run is what caught the hole**: with routing unit-tested and green, an end-to-end run showed the judge still on the builders' model — a no-frames judge takes a third spawn path (`judge_node`'s inline args) that neither reserved key reached. Confirmed fixed in a real core+editor run: coder x3 -> `mock-coder-m3`, artist -> `mock-artist-m3`, unmapped tester -> default, judge x2 -> `mock-judge-luna`, monitor x5 -> `mock-monitor-mini`, parent x7 -> the picker's model. Seven tests. 597 Rust tests green. Documented in `docs/runbook.md`. |
| CALI-SOAK-032 | P1 | Activity transcript | An agent turn's expanded actions dumped every tool result as always-open JSON, so one `loop_report_open` result buried the next ten actions, and a failed call rendered with the same gray check as a success — the `loop_report_start` error in the user's screenshot read as passed. | Fixed: each action is a click-to-expand row (result JSON, file link, and diff fold away until asked for), and an errored action carries a red `X`, red title, and red detail, mirrored on the turn row when a finished turn's last action failed. 601 client tests green; verified by headless render of both states. |

| CALI-SOAK-033 | Verified | One prompt to loop, in the editor | The product claim — type one prompt, get a fanned-out judged loop with per-iteration progress visible while it runs — had only ever been checked in pieces, over RPC. Nobody had watched it happen in the editor. | Verified end to end in an isolated core+editor (`:8799`/`:5233`) against `scripts/mock-model.py graph`: one composer prompt produced 7 parent turns, 5 graph nodes, 5 monitor calls and 2 judge calls; the graph panel shows Integration x2 and Blind judge x2 (the re-queue), all PASSED at 95/90 vs the named reference; the sidebar spinner is present for the whole run; the REPORTS tab renders live (`Running - loop-mock-8811 - Live`, 2 iterations, 95%, 6/6 checks, 6 agents) with per-iteration scores, checks and agent fan-out. Two guards fired correctly and loudly: an iteration predating its report is refused (`iteration startedAtMs cannot precede the loop`), and completion is refused with `iteration 2 has no visual evidence` — the mock claims passing checks but attaches no frames, so the report stays `running`. Not exercised: contact sheets reached no judge in this run (0 images), because mock nodes capture nothing; that path stays unit-tested only. |
| CALI-SOAK-034 | P2 | Loop-test fidelity | `scripts/mock-model.py` reused the tool-call id `call-0` for every call, and the client pairs activity rows by call id — so a later failure landed on an earlier row's label (`Used loop_report_start` shown in red for a failed `loop_report_update`). A test harness that manufactures a UI symptom is worse than none. | Fixed: globally unique call ids, real wall-clock iteration timestamps, per-request `model` logging (which is how role routing was verified live), and the `graph` scenario now drives the whole documented /loop shape — start report, plan, run, one iteration per pass, close out. |

| CALI-SOAK-035 | Verified | Frames reach the graders | CALI-SOAK-033 proved the loop runs from one prompt but graded no pixels: mock nodes captured nothing, so every monitor and judge call carried 0 images and the whole contact-sheet path stayed unit-tested only. The claim that judging is visual had never been observed. | Verified end to end with build nodes that actually play and capture (`editor_run_pie` -> 3x `editor_capture_frame` -> `editor_persist_capture`). Result in one run: monitor 5/5 calls carried a sheet, judge 2/2 carried one, and core wrote 5 sheets + 5 manifests under `reports/video/` (one per node per attempt, including the re-queued integration attempt 2). A manifest reads `columns: 4, tile 320x320, sheet 1310x368` with per-tile `frame_number` and `timestamp_seconds` (0.00/0.08/0.17/0.37s), and the rendered sheet carries visible `1 T 0.00S` style labels — time as position on the page, which is what lets a judge reason about motion. Captures are faithful: the tiles match a direct screenshot of the live viewport (pale hero cube on a light floor). Role routing held at this larger scale too: coder x12, artist x4, unmapped tester x4 default, judge x2 -> `mock-judge-luna`, monitor x5 -> `mock-monitor-mini`. |
| CALI-SOAK-036 | Verified | Loop completion never demonstrated | The success path of `loop_report_update` is still unobserved. Iterations are appended with no `evidence` entries, so completion is refused (`iteration 2 has no visual evidence`) and the report ends `running` with 2 iterations. Only the refusal has ever been seen; nothing proves a legitimately-evidenced loop can actually close. Verified: with iterations citing the paths the build nodes actually persisted (`reports/frames/frame-N.png` via `editor_persist_capture`), one prompt now drives a loop to `status: completed` with a real summary, 2 iterations and 4 evidence entries each. Getting there cost two more fixture-vs-guard collisions, both of which core won: an iteration dated before its own report is refused, and an iteration whose completion is *future*-dated makes the loop's own `completedAtMs` precede it (`completedAtMs cannot precede an iteration completion`). The completion gate reads the **latest** iteration's average, so a 70-then-95 loop can close while a still-failing one cannot. |

| CALI-SOAK-037 | P1 | Report evidence in the editor | A finished loop's cited frames were visible only in the standalone `report.html`. The editor's REPORTS tab rendered each evidence item as a caption plus a filename button — so the one place built for watching progress live was the one place the frames could not be seen, and reading them meant leaving for the Code tab. | Fixed: image evidence renders as a thumbnail from `/projects/<slug>/<path>` (the route core already serves), keeping the file button for open-in-Code. Non-image kinds and unsafe/traversal paths render no tile, and a missing file hides itself instead of leaving a broken image. Verified live: four frames per iteration decode in the editor at `naturalWidth: 559`, zero console errors. Two tests; 654 client tests green. |

| CALI-SOAK-038 | P1 | Context and cache | `editor_capture_frame` returned its full `data:image/...` URL into the calling agent's transcript. Core harvests frames from the tool *event* to build the contact sheet, so the transcript copy was read by nobody — while being re-sent on every later turn of that agent, evicting the provider's cached prefix, and eating most of a small model's context. Measured: one node turn carried 220,229 base64 chars in a 264,264-char request (83% of the body), and prefix reuse for that call fell to 0.163 against a 0.994 median. | Fixed in `bound_tool_result`: any `data:image/...` string over 512 chars becomes a receipt naming `editor_persist_capture(path)` as the way to keep a frame; short schema examples are left alone. Same workload after: worst node body 264,264 -> 45,162 chars, base64 in node transcripts 220,229 -> 23, node prefix reuse min 0.163 -> 0.972 — and sheets still reach 5/5 monitors and 2/2 judges as `image_url` parts, so nothing the graders are meant to see was touched. Two tests; 599 Rust tests green. Analysis in `docs/prompt-cache.md`. |
| CALI-SOAK-039 | Verified | Prefix hygiene | Whether this harness *could* reach a Pi-style ~99% cached input had never been measured; the earlier caching work (CALI-SOAK-026) only covered provider signalling, which is worthless if the prefix itself moves. | Measured with per-request prefix-reuse instrumentation in `scripts/mock-model.py`. The tool schema block is byte-identical across every call (35,249 chars, 1 distinct payload in 7 parent calls — `HashMap` order does not reach the wire), the system prompt is stable per role, and growth is append-only. Post-fix reuse: build subagents 0.972/0.990/0.994 (min/median/max), top-level chat 0.877/0.914/0.970 — the parent's lower figure is transcript-growth arithmetic, not instability, and improves as a session grows. |

| CALI-SOAK-040 | P1 | /loop memory | Carry-forward between `/loop` iterations was *instructed*, not delivered: iteration N's prompt said "Read loop_report_open first and use its nextIterationMemory plus punch list". A model that skips that call loses the entire carry silently, and the DONE gate only checks that memory *exists* in the report, never that a later iteration used it — so a loop could repeat a fixed defect and still complete. | Fixed: the panel reads the report between iterations and inlines the latest slice — unresolved punch-list items (resolved ones are dropped, since re-listing them invites redoing work) plus nextActions/decisions/risks/observations, capped at 6 items per group and 1,200 chars. The durable report stays the source of truth and the prompt says so ("open the report only if you need detail it omits"); a failed read logs and falls back to the old instruction rather than stalling the loop. Matches the field's file-backed pattern (Ralph-style loops keep state in files across clean contexts) while removing the compliance gap. Three tests; full client suite green. |

| CALI-SOAK-041 | P2 | Multi-day progress visibility | "See progress daily" across a soak was served by a `<select>` reading `status: objective`. On a soak where every loop shares one objective, yesterday's run and today's are the same string — no date, no score, no size, so the list showed history without showing trajectory. | Fixed: each entry now reads `Aug 12 · Completed · 3 iter · 95% · 4 files — <objective>`. The judge score is the trajectory line: it is the one number that says whether the previous day's work moved the bar. One test; 657 client tests green. |

## Passing baseline from the same run

- New project creation and template selection persisted a valid project.
- Scene edits and script edits survived core restart.
- Native PIE ran at 59–60 FPS.
- Play, Reset, and the existing two-test suite passed; the final run captured
  10 frames and reported no issues.

## Exit criteria

1. Only one deterministic editor owner exists per active project/session, or
   contention is surfaced with a direct recovery action.
2. Agent terminal states preserve a useful reason and retry path; tool-only
   progress cannot end as an unexplained empty response.
3. Signed and decimal transforms are safe to type and remain editable at
   supported window sizes.
4. Desktop builds do not trigger Vite reload storms, and core restart does not
   silently replace the current project.
5. The full verification suite passes, followed by repeated native-app
   create/edit/agent/PIE/reset/test/reload loops without a blocking failure.
